# ⭐ v2026.6.5

## September 2, 2026

### New

* Branded access messages — Custom Zephyr pages for situations where Cloudflare blocks or restricts access (for example, security rules, geographic restrictions, or WAF blocks). Visitors see on-brand guidance and support options instead of a generic error.

### Fixes

* Homepage structured data — Corrected how JSON-LD structured data is embedded on the homepage so search engines can read organization and product metadata reliably.

### Improvements

* Reliability — Backend and deployment tooling updates to keep submodule installs and API services running smoothly in production.
